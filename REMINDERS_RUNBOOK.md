# دليل تشغيل التذكيرات وFollow-up

## الحالة الحالية

نظام الجدولة الخلفية في الإنتاج مفعّل. وظيفة `send-reminder` تعمل كل 15 دقيقة لإرسال التذكيرات المستحقة، ووظيفة `enqueue-physio-followups` تعمل يوميًا عند **01:30 UTC** لإنشاء تذكيرات المتابعة المستحقة لحالات العلاج الطبيعي. إنشاء التذكير idempotent؛ أي أن تشغيل الجدولة أكثر من مرة لا ينشئ نسخة مكررة لنفس الحالة والاستحقاق.

القنوات ليست كلها جاهزة افتراضيًا. **Push / in-app** يعمل داخل النظام، بينما WhatsApp وSMS يحتاجان تفعيلًا ومفاتيح مزود صحيحة من إعدادات الفرع. Email لا يُعتبر قناة إرسال فعلية ما لم يتم ربط مزود بريد حقيقي؛ النظام الآن لا يسجل Email على أنه `sent` لمجرد عدم وجود خطأ برمجي.

## تشغيل WhatsApp

افتح **Settings → Communication → Notifications**، ثم اختر WhatsApp كقناة التذكير. فعّل WhatsApp، واختر مزود الإرسال المتاح في حسابك، مثل Twilio أو Meta أو Custom، ثم أدخل بيانات المزود المطلوبة واحفظ الإعدادات.

بعد الحفظ، نفّذ اختبارًا على تذكير تجريبي برقم هاتف مصرح به. راجع نتيجة التذكير في سجل الرسائل، ويجب أن تكون الحالة `sent` مع معرف الرسالة من المزود. إذا ظهرت `disabled` أو `provider not configured` فالقناة لم تُفعّل أو لم تُحفظ بيانات المزود كاملة.

## تشغيل SMS

من الشاشة نفسها اختر SMS، فعّل القناة، اختر المزود، وأدخل بيانات الاعتماد ورقم الإرسال أو Sender ID وفق متطلبات المزود. احفظ الإعدادات ثم اختبر رسالة واحدة إلى رقم مصرح به.

يجب عدم اعتبار الرسالة ناجحة إلا إذا عاد مزود SMS بنتيجة إرسال حقيقية. أخطاء مثل `SMS provider not configured` تعني نقص الإعدادات وليست عطلًا في جدولة النظام.

## تشغيل SMS Misr داخل ZMedico

بعد تحديث النسخة المنشورة، افتح **Settings → Communication → Notifications**، اختر **SMS**، فعّل SMS، ثم اختر **SMS Misr** من قائمة المزودين. ستظهر حقول SMS Misr التالية:

| الحقل | القيمة في حساب SMS Misr |
|---|---|
| SMS API URL | `https://smsmisr.com/api/SMS/` |
| Username | Username من إعدادات SMS Misr |
| Password | Password من إعدادات SMS Misr |
| Test Sender Token | الـSender Token التجريبي الذي يوفره SMS Misr |
| Environment | اختر `Test (2)` أولًا، ثم `Live (1)` بعد اعتماد الحساب |
| Message Language | `English (1)` أو `Arabic (2)` أو `Unicode (3)` |

احفظ الإعدادات ولا تضع كلمة المرور أو Sender Token في ملفات المشروع. استخدم رقمًا مصريًا مصرحًا به بصيغة دولية مثل `2010xxxxxxx`، ثم أنشئ تذكيرًا واحدًا واضغط **Send reminder**. يجب أن تعود استجابة SMS Misr بالكود `1901` حتى تُسجّل الرسالة كـ`sent`. الأكواد `1903` و`1904` و`1905` و`1906` تعني على الترتيب بيانات دخول غير صحيحة، Sender غير صحيح، رقم غير صحيح، أو رصيد غير كافٍ.

لا تستخدم **Send all pending** أثناء الاختبار. بعد نجاح الرسالة التجريبية، يمكنك تحويل Environment إلى `Live (1)` فقط بعد التأكد من Sender ID والرصيد والمسار المصري، ثم اختبار رسالة واحدة أخرى قبل تشغيل التذكيرات الجماعية.

## التذكيرات داخل النظام

التذكير يُنشأ من الشاشات التي تدعم مواعيد المتابعة أو الإشعارات، ثم يلتقطه مجدول `send-due-reminders` كل 15 دقيقة. Push / in-app يظهر داخل النظام للمستخدمين المخولين، أما الإرسال الخارجي فيتطلب قناة ومزودًا جاهزين.

إذا فشل الإرسال، لا تحذف السجل مباشرة. افحص سبب الفشل في سجل الرسائل، صحح إعداد القناة أو بيانات الاتصال، ثم أعد المحاولة وفق صلاحيات المستخدم. السجلات السابقة التي فشلت بسبب إعداد غير مكتمل لا تعني أن الكود معطل.

## Follow-up العلاج الطبيعي

من **Physio → Case detail → Follow-up** فعّل المتابعة للحالة، وحدد تاريخ الاستحقاق أو قواعد المتابعة المتاحة، ثم احفظ الحالة. في التشغيل اليومي، تلتقط `enqueue-physio-followups` الحالات التي أصبحت مستحقة وتنشئ تذكيرًا واحدًا لكل حالة ولكل استحقاق.

سير العمل الكامل هو: إنشاء المريض، فتح حالة علاج طبيعي، تعيين المعالج، إضافة الجلسات والحضور، تسجيل إعادة التقييم عند الحاجة، تفعيل Follow-up وتحديد الاستحقاق، إنشاء التذكير تلقائيًا، ثم إرسال التذكير عبر القناة الجاهزة أو عرضه داخل النظام كإشعار in-app.

لا يُنشأ Follow-up للحالة المحذوفة أو غير المفعّلة للمتابعة أو التي لا تملك تاريخ استحقاق صالحًا. كما يمنع فهرس idempotency تكرار التذكير عند إعادة تشغيل الوظيفة اليومية.

## الجدولة الخلفية

| العملية | الجدول | الوظيفة | الغرض |
|---|---:|---|---|
| إرسال التذكيرات المستحقة | كل 15 دقيقة | `send-reminder` | معالجة queue وإرسال الرسائل |
| إنشاء Follow-up العلاج الطبيعي | يوميًا، 01:30 UTC | `enqueue-physio-followups` | إنشاء تذكيرات الحالات المستحقة |
| Win-back | يوميًا، 02:00 UTC | `enqueue-winback-daily` | إنشاء تذكيرات العملاء غير النشطين |

توقيت 01:30 UTC يساوي 04:30 بتوقيت القاهرة الصيفي عند تطبيق UTC+3. لا يحتاج المستخدم إلى فتح الموقع لكي تعمل الجدولة، لكن يجب أن تكون بيانات الحالة والقناة محفوظة قبل موعد التشغيل.

## المتطلبات السرية والتشغيلية

يجب أن يكون GitHub Secret باسم `SEND_REMINDER_CRON_SECRET` مساويًا للقيمة الموجودة في Supabase Vault أو إعدادات الوظيفة المستخدمة لاستدعاء الإرسال. لا تُحفظ هذه القيمة أو مفاتيح Twilio أو Meta أو SMTP داخل المستودع أو داخل هذا الملف.

للتأكد من الجاهزية، راجع بطاقة حالة الإرسال في شاشة Notifications، ثم أنشئ حالة اختبار أو موعدًا قصير الأجل، وانتظر دورة الجدولة، وتحقق من سجل الرسالة. الجاهزية الحقيقية لقناة خارجية تعني وجود مزود محدد، مفاتيح صحيحة، رقم إرسال معتمد، ورقم مستلم صالح، وليس مجرد تفعيل مفتاح القناة.

## تفسير حالات الفشل الحالية

| الحالة | التفسير | الإجراء |
|---|---|---|
| `SMS not configured` | لا توجد بيانات مزود SMS مكتملة | اختيار المزود وإدخال المفاتيح ثم الحفظ |
| `SMS Misr not configured` | Username أو Password أو Sender Token غير محفوظ | إدخال بيانات SMS Misr وحفظها، مع إبقاء Environment على Test أثناء التجربة |
| `SMS Misr 1903/1904/1905/1906` | بيانات الدخول أو Sender أو رقم الهاتف أو الرصيد غير صحيح | راجع حساب SMS Misr والرقم والرصيد ثم أعد الاختبار |
| `WhatsApp disabled` | قناة WhatsApp غير مفعّلة للفرع | تفعيل القناة ثم حفظها |
| `no phone` | المريض لا يملك رقم هاتف صالحًا | إضافة رقم صحيح وموافقة الاستخدام |
| `provider not configured` | القناة مفعّلة لكن المزود أو بياناته ناقصة | استكمال إعداد المزود |
| `Email provider not configured` | لا يوجد مزود بريد حقيقي | ربط SMTP أو مزود بريد transactional |

## قائمة تحقق قبل البيع

قبل تشغيل العيادة فعليًا، اختبر حالة واحدة على الأقل لكل قناة ستستخدمها، وراجع وصول الرسالة من جهاز المستلم، وتأكد من ظهورها في سجل الرسائل بالحالة الصحيحة. اختبر أيضًا إعادة تشغيل وظيفة enqueue للتأكد من عدم إنشاء تكرارات، واختبر أن حالة Follow-up غير المفعّلة لا تنتج تذكيرًا.

إذا كانت العيادة ستبدأ بـPush / in-app فقط، فهذا مناسب للتشغيل الداخلي، لكنه لا يُسوّق للعملاء على أنه SMS أو WhatsApp حتى يتم ربط مزود خارجي واختباره فعليًا.
